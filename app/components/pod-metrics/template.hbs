<section>
  {{metrics-action 
    queryAction="query"
    state=state
  }}
  {{#unless state.hide}}
    {{metrics-graph graphs=graphs loading=state.loading noGraphs=state.noGraphs}}
    <hr class="mt-30"/>
  {{/unless}}
</section>